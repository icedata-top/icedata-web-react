# icedata-web-react
基于 React 的 icedata 前端项目

## Quick start
1. 安装依赖：`npm install`
2. 启动开发服务器：`npm start`
3. 打包构建：`npm run build`

启动后默认在 `http://localhost:5173/` 访问。

## 配色方案

冰数据主题配色 #438BED
高亮主题配色 #76AFF2